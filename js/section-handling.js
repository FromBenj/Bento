function settleSectionTitle() {
    const sectionsTitle = document.querySelectorAll('.panel-section-title');
    if (!sectionsTitle.length) return;

    return '';
    // const {bentos: savedBentos = []} = await chrome.storage.local.get('bentos');
    // for (const bento of savedBentos) {
    //     await addBentoToBentoArea(bento.id, bento.name, bento.url);
    // }
    //
    // for (const sectionTitle of Array.from(sectionsTitle)) {
    //     const titleText = sectionsTitle.innerText;
    //     const titleData
    // }
}
